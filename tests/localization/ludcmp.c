double fabs(double n)
{
	double          f;

	if (n >= 0)
		f = n;
	else
		f = -n;
	return f;
}

int  main(void)
{
	double	d;
	int             i, k;
	
	d = fabs(d);
	for (k = 0; k < i; k++)	;

	return 0;
}

